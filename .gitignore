.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn

# Never commit keypairs — including the program keypair under target/deploy.
*.pem
keypair.json
id.json

# Env
.env
.env.local
.env*.local
!.env.example

# Next.js
frontend/.next
frontend/out
frontend/build
frontend/.vercel
frontend/tsconfig.tsbuildinfo
.claude/

# The rollback binary and its IDL: 530 KB of build output, kept on disk
# beside the notes that explain them, not in the history.
ops/rollback/*.so
ops/rollback/*.idl.json
